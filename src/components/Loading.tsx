import TailSpin from "react-loading-icons/dist/esm/components/tail-spin";

const Loading = () => {
    return (
        <div>
            <TailSpin stroke="currentColor" />;
        </div>
    );
};

export default Loading;
